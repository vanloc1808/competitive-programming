#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

#define fastinout ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)

int main()
{
	fastinout;
	int t;
	cin >> t;
	while (t--)
	{
		int x;
		cin >> x;
		cout << 1 << " " << x - 1 << "\n";
	}
	return 0;
}
