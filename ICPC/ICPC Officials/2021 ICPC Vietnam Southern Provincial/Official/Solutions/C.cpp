#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;

    int k;
    cin >> k;

    vector<int> a;

    for (int i = 0; i < s.length(); i++) {
        a.push_back(s[i] - 'a');
    }

    
}